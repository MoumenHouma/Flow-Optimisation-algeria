// Optimization screen — docs/DESIGN.md §3.3 (params, result map, route details).
export function OptimizationPage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-bold">Optimisation</h1>
      <p className="mt-2 text-neutral-500">
        TODO: panneau paramètres (véhicules, contraintes), carte résultat, détails des
        tournées, export PDF/Excel (docs/DESIGN.md §3.3). Utilise useOptimize + useOptimizationJob.
      </p>
    </main>
  );
}
