// Fleet management — vehicles CRUD with capacity + depot (F7).
export function FleetPage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-bold">Flotte</h1>
      <p className="mt-2 text-neutral-500">
        TODO: liste des véhicules, ajout/édition (capacité poids/volume, type, dépôt),
        respect du quota de plan (docs/PRD.md §5.1, F7).
      </p>
    </main>
  );
}
