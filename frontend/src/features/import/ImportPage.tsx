// CSV/Excel import — docs/DESIGN.md §3.5 (drag-drop, preview table, validation).
export function ImportPage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-bold">Importer les livraisons</h1>
      <p className="mt-2 text-neutral-500">
        TODO: zone de dépôt CSV/Excel, prévisualisation, validation des adresses
        (matched/approximate/failed), correction (docs/DESIGN.md §3.5, F1/F2).
      </p>
    </main>
  );
}
