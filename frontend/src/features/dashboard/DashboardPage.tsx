// Manager dashboard — docs/DESIGN.md §3.2 (KPIs, map, active routes, weekly perf).
export function DashboardPage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-bold">Tableau de bord</h1>
      <p className="mt-2 text-neutral-500">
        TODO: KPI cards (distance, temps, livrées, véhicules actifs), carte des livraisons,
        tournées actives, performance hebdomadaire (docs/DESIGN.md §3.2).
      </p>
    </main>
  );
}
